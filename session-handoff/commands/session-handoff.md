---
description: Generate a concise HANDOVER.md so the next Claude session can resume without losing context — uses lazy references, not loaded content
---

Invoke the `session-handoff` skill to create or update the session handover document.
